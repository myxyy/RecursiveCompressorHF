## やること
[Mambaの論文](https://arxiv.org/abs/2312.00752)のFigure. 2にある3つのタスクのうちまず"Copying"をやりたい

## タスク
基本的に https://arxiv.org/abs/2102.02611 の"Copy Memory Problem"を踏襲する
* 語彙は0~9のdigit（語彙数10）
* 入力長はTをパラメータとしてT+20
  - 最初の10文字は1~8のランダム
  - 次のT-1文字は0で埋める
  - 末尾11文字は9で埋める
  - ターゲットは先頭T+10文字を0埋め、末尾10文字を入力の先頭10文字とする

## やりたいこと、考えることの詳細
### モデル
* 次元数 512くらい？
* FFN隠れ次元は次元数2倍で
* ブロック数は2が標準みたい？

### 学習
* 学習時はT=1~決めた最大コンテキスト長-20でランダム、最大コンテキスト長をどうするか、2048くらい？
* 勾配伝搬どうする？
  - いろんなTに対して学習することを考えると便宜上勾配伝搬しないPADトークンを追加するのがよい？
  - 末尾10文字に対してのみ勾配伝搬するとすれば0をPADトークンとできるが既存研究との整合性は？
* データセットサイズ？
* エポック数？

### 検証
* 学習時よりも長いTに対して汎化するかを見たい
  - T=1~131072くらいでたくさん試す
  - 正答率のグラフを作る
    + 横軸がT、縦軸が正答率、T=1024（仮）のところに縦線引いて学習時ホライズンを示す

### スクリプト
* 訓練と検証のスクリプトは分けたい
  - `exp/copying/`とかディレクトリを作ってそこに置きたい、人工データセット系検証は`exp/`ディレクトリに置く
  - 重みは`DATA_DIR`に置く

---

# 計画 (Claude追記)

## 関連論文の確認結果
* **CKConv (arXiv:2102.02611)** のCopy Memory Problem定式は本ドキュメントの仕様と一致することを確認:
  先頭10文字が{1..8}ランダム、次のT-1文字が0、**末尾11文字が9**(delimiter)、総長T+20。
  ターゲットは末尾10位置で先頭10文字を再現、**それ以外の位置では0を出力**(=全位置で損失を取る)。
* **Mamba (arXiv:2312.00752)** Fig.2: 標準Copyingは入出力の間隔が一定なため「時間認識(time-awareness)だけで解ける」
  (LTIモデルでも可能)。content-aware性が要るのはSelective Copying。まずCopyingでベースライン能力を確認し、
  次段階でSelective Copyingに進むのが自然な順序。合成タスクは2層・d_model 64〜128の小モデルで行われている。
* **Repeat After Me (arXiv:2402.01032)**: コピーの長さ汎化は位置埋め込みに強く依存
  (絶対位置埋め込みは訓練長で急落、NoPE/ALiBi系は外挿しやすい)。評価はtoken accuracyと
  string accuracy(10文字完全一致)の両方を見るのが標準。
  RecursiveCompressorは絶対位置埋め込みを持たない(チャンク内周期pos_emb + 階層構造の暗黙位置のみ)ため、
  **長さ外挿に構造的に有利という仮説が立つ — 本検証の見どころ**。

## 設計判断
### タスク/損失
* 入出力は**同位置対応のseq2seq形式**(RNN文献の標準)。`RecursiveCompressorLM.forward(input_ids, labels)`は
  同位置でlogitsとlabelsを比較する実装なので、シフトなしでそのまま使える(causalなので位置tは入力0..tを参照可能)。
* 損失は**全位置CE**(0埋め位置も「0を出力」として学習)をデフォルトとする。CKConv等の既存研究と整合。
  比較用に末尾10位置のみ(-100マスク)も`--loss-positions answer`オプションで残す。
* PADトークンは追加しない: **バッチ内でTを固定**(バッチごとにTをサンプル)すれば全サンプル同長でパディング不要。
  語彙数はちょうど10のまま。

### モデル (デフォルト)
* d_model=512, num_heads=8, d_ff=1024, num_layers=2, chunk_size=4, compress_size=1 (~11Mパラメータ)
* compress_size=1が最も厳しい情報ボトルネック(10桁≈33bitを1ベクトルの再帰圧縮で運ぶ)。
  失敗時はcompress_size=2,4のablationで容量起因かを切り分ける。

### 学習
* **オンライン生成**(毎バッチ新規サンプル、エポック概念なし)。データセットサイズの問いは消滅、
  ステップ数で管理。再現性はseed固定で担保。
* 訓練T ~ U[1, 2028] (最大コンテキスト2048-20)。バッチ内T固定、バッチごとに再サンプル。
* バッチサイズ64、50kステップを初期値に(短いTのバッチは軽いので平均的には速い)。
* 単GPU・AdamW(lr 3e-4, warmup 1k, その後cosineなし固定)をデフォルトに。
  本番のMuon+ReduceLROnPlateauは持ち込まない(検証の変数を減らす。既存研究もAdam系)。
* bf16 autocast (本番と同じ数値環境)。

### 検証
* T grid: {1..14の全点} ∪ {16, 24, 32, 48, 64, ..., 131072}(2^kと中間点)。各Tで256サンプル。
* メトリクス: **token accuracy**(末尾10位置)と**string accuracy**(10文字完全一致)の両方をJSON保存。
* プロット: 横軸log T、縦軸accuracy、訓練ホライズン(T=2028)に縦線。token/string両曲線。PNG保存。
* 評価はforward一括(hiddenの逐次生成は不要、教師強制でlogits argmaxを取るだけ)。
  チャンクattentionなのでT=131072でもO(L)メモリ、batch縮小で対応。

### 事前に必要なコード修正
* **不要と判明(訂正)**。`_num_queries()`の`log(65536)`ハードコードは当初「系列長65536超で
  クエリスロット不足」と考えたが、検証の結果**現アーキテクチャでは機能的に無害**:
  - スロットは全てNoneプレースホルダで、再帰中に尽きてもcomp_query=None→チャンク末尾
    ベクトルからの導出で継続する(レイヤ0の全レベルと同じ経路)。出力リストは実深度まで
    伸長するのでレイヤ間クエリ伝播も全深度で保たれる
  - 実測: スロット数9→2に減らしてもlogits完全一致(diff=0)、T=131072(系列長131092)の
    一発forwardも正常動作(小モデルで0.3秒)
  - 旧アーキテクチャ(学習可能クエリを上から配る方式)の遺物であり、いずれ削除してよい
* 評価時のメモリが厳しい場合は`step()`によるチャンク分割推論(hidden引き継ぎ)が使える
  (フルforwardとの差は丸め誤差レベルであることをT=131072で実測確認済み)。
  hiddenはO(log L)と極小なので長いTでも評価バッチを大きくできる。

## ファイル構成
```
exp/copying/
  task.py       # サンプル生成 (make_batch(T, batch_size, rng) -> input_ids, labels)
  train.py      # 訓練ループ。--max-t, --steps, --batch-size, --loss-positions, --compress-size等
  evaluate.py   # チェックポイントを読み、T gridで評価してJSON + PNG出力
```
* チェックポイント/結果は `$DATA_DIR/exp/copying/` 配下 (run名サブディレクトリ)。
* モデルは`RecursiveCompressorLM`をそのままimport(configで小型化)。訓練コードは独立した
  単GPUループ(train_pipeline.pyは流用しない)。
* `task.py`にはユニットテストを追加(フォーマット検証: 長さT+20、各領域の記号、ターゲット整合)。

## 進め方
1. ~~`_num_queries`のconfig化~~ (不要と判明、上記訂正参照)
2. `task.py` + フォーマットのテスト → **完了** (exp/copying/task.py, test_task.py 12テスト)
3. `train.py` → **完了** (単GPU、オンライン生成、bf16 autocast、quick_eval付き)
4. `evaluate.py` + プロット → **完了** (チャンク分割step推論、results.json + plot.png)
5. 本実験: T~U[1,2028]で訓練 → T=1..131072で評価 → グラフ
6. (結果を見て) compress_size / 損失位置 / 訓練T分布のablation、次段階としてSelective Copying

## スモークテスト結果 (2026-08-16, 重要な発見)
小型モデル(d=128, 2層)によるスモークで、**現アーキテクチャはT=1(系列長21)ですらコピーを
解けない**ことが判明。実装のバグではなく構造的な挙動と考えられる:

* T~U[1,16]・全位置loss・6000step: token acc 0.116 = チャンス(1/8)に完全に張り付き
  (「answer位置で一様分布を出す」局所解。lossはその理論値0.73と一致)
* T=1固定・answer-only loss: 学習は立ち上がる(0.11→0.52)が**0.52で飽和**。
  15000stepまで回しても0.5196で微動せず(grokking的な遅延立ち上がりではない)
* compress_size=1→8で結果が**全く変わらない**(0.516) → 記憶容量の問題ではない
* 桁別の内訳: **memory[0]とmemory[9]は100%、中間桁は0.29-0.50** —
  階層圧縮が「桁の順序・位置の同一性」を保持できないbag-of-tokens的挙動
* 感度分析(ランダム初期化): 先頭10桁→末尾10位置のlogit感度はT=1で0.43、
  **再帰1階層ごとに約1/10に指数減衰**(T=64で0.001)。因果的には届くが信号が薄い

### 示唆される構造的原因(仮説)
1. **階層をまたぐ位置情報の欠如**: pos_embはチャンク内4周期のみ。マーカー領域のチャンク
   (9,9,9,9)は内容が同一で、「自分が何番目の出力チャンクか」をouter context経由でしか
   知り得ない。圧縮列に位置情報を付与する機構がない
2. **attention圧縮の置換不変性**: クエリ1個のattention圧縮は加重平均であり、
   チャンク内pos_emb以外に順序を保つ帰納バイアスがない。先頭/末尾だけ取れて中間が
   混ざるのは典型的な症状
3. 信号の指数減衰(gated attentionのsigmoid≈0.5が階層ごとに掛かる等)

### 次の一手の候補
* 圧縮列(レベル1以上)への位置情報付与(レベル別pos_emb、または圧縮時に系列位置を注入)
  - ※レベル別pos_embは「メモリの許す限り無限に生成する」という設計目標と相性が悪いので避けたい。
    レベル**共通**の相対位置バイアス(ALiBi的、圧縮attention/decompressorに適用)なら長さ非依存で両立可能
* decompressorのkvを「直前チャンクの圧縮1点」から「過去数点」に広げる(局所検索の強化)
* ~~gateの初期バイアスを開く(sigmoid≈1で初期化)ことで信号減衰を緩和~~ → **検証済み、微改善のみ(下記)**
* まず自然言語で性能が出ている理由との整合を考える(局所n-gram + 大意の圧縮で
  十分なタスクでは順序保持の弱さが顕在化しない)

### ALiBi化の検証結果 (2026-08-16)
`compressor_query_pos`を削除し、ALiBi相当のヘッド毎スロープ(2^(-8h/H)、固定・非学習)を
attention logitに加算する方式に変更:
* encoder(チャンク内causal self-attn): 標準ALiBi `-m_h*(i-j)`、causalマスクは-infで焼き込み
* compressor(圧縮クエリ→チャンク内4点): kv位置のチャンク末尾からの距離 `-m_h*(C-1-j)`
* decompressor: kvに位置構造がないため適用外
* バイアスはチャンク内相対距離のみの固定バッファで全レベル共有 → **長さ非依存、無限生成と両立**
* 注意: バッファはpersistent必須(persistent=Falseだとfrom_pretrainedのmeta-device初期化で
  未初期化になる HFの落とし穴を踏んだ)

結果:
* ランダム初期化の感度: ゲート開放比でさらに2倍前後改善(T=16: 0.024→0.053, T=256: 0.0001→0.00035)
* **T=1学習: 0.528で飽和 — 壁は破れず**(ベースライン0.516/ゲート開放0.530と同水準)
* 消去法更新: 容量✗、ゲート減衰✗、attention logitへの位置バイアス✗
* 次の仮説: 記憶10桁がチャンク境界(4)と非整合(10=2.5チャンク)で、出力位置も
  チャンク途中(11)から始まるため、「チャンク粒度のouter要約」と「桁単位でスライドする
  入出力対応」が噛み合っていない可能性。出力チャンクごとに必要な4桁の窓がずれる
  (c3→m1-m4, c4→m5-m8)が、outerはチャンク単位でしか変化しない。
  → decompressorのkv拡張(過去k点の圧縮を見せて桁単位の検索を可能にする)が次の本命

### kv拡張(retrieve_size)の検証結果 (2026-08-16)
decompressorのkvを「直前チャンクの圧縮1点」→「過去retrieve_size(=k)チャンクの圧縮列」に拡張:
* `retrieve_size`をconfigパラメータ化(デフォルト4)、`initial_context`を(k, S, d)に拡張(学習可能)
* 窓は`cat([prev_outer, compressed_out])`からのunfoldスライシングで実装。先頭チャンクは
  initial_contextとの混合窓になり特別分岐なし。hiddenのouterは直近k点(B, k, S, d)
* decompressorにチャンク距離のALiBiバイアス`-m_h*(k-1-w)`を追加
* step/forward等価性は窓境界をまたいでも維持(全74テストパス)

結果: **T=1で0.5295 — 壁は不変**。cs=4+k=4の組でも0.53。

### 重要な発見: compress_sizeクエリの縮退
compress_size(S)>1のクエリはS本とも「チャンク末尾ベクトル由来の同一値」で、compressorの
ALiBiバイアスもS軸にexpandした同一値のため、**S本のクエリが完全に縮退して実質1本**になっている
(残差もattention入力も同一 → 出力も同一)。compress_size=8で結果が変わらなかった過去の実験も
これで説明がつく。**S>1は現状机上のパラメータで、圧縮容量は常にd_model 1本分**しかない。

修正案(次の一手): compressorのALiBiバイアスをクエリ別にして対称性を破る —
`bias[h,s,j] = -m_h * |c(s) - j|`、`c(s) = s*(C-1)/(S-1)` (クエリsがチャンク内位置c(s)近傍を
担当)。S=C=chunk_sizeなら各クエリが1トークンずつ担当し、圧縮が順序保存の再配列に近づく。
decompressorはk*S点の窓から素直なattention検索で任意の桁を取り出せるようになる。
学習可能パラメータ追加なし・長さ非依存でALiBiの設計思想とも整合。

### クエリ縮退解消の検証結果と真のボトルネック特定 (2026-08-16)
compressorのALiBiバイアスをクエリ別に一般化(クエリsが担当位置c(s)=s(C-1)/(S-1)近傍を選好、
`-m_h*|c(s)-j|`。S=1は従来の末尾距離とビット一致)。S=4でクエリの縮退は解消したが、
**T=1は0.5295で壁は不変**。

per-digit分析で決定的なパターンを確認:
* m0(チャンクc2担当)とm9(c5担当): **100%** — そのチャンクが出すべき答えが1桁だけのケース
* c3担当のm1-m4、c4担当のm5-m8: 0.37-0.44で均等に低い — 1チャンクが4桁を出すケース
* 検算: (2×1.0 + 8×0.41)/10 = 0.528 ≈ 観測値0.5295。全構成が同値に収束した理由も説明可能

**真のボトルネック: decompressor(cross-attn)がencoder(self-attn)より前にあり、
クエリが位置未分化の生埋め込みであること。** markerチャンク(9,9,9,9)内の4位置は
クエリが完全に同一 → decompressorから全員が同じ情報しか受け取れない → チャンクあたり
実質1本(d_model)の情報で4桁を賄うことになり、encoder後の位置分化では取り分けきれない。
容量・ゲート・位置バイアス・層数・kv窓・クエリ縮退の全介入が無効だったのは、
どれもこの「クエリの位置盲目性」に触れていなかったから。

修正案(次の一手): **処理順をencoder(チャンク内causal self-attn) → decompressor(cross-attn)
に入れ替える**。標準Transformerデコーダブロックの順序(self-attn → cross-attn → FFN)と同じ。
encoderのALiBiで位置分化したクエリが窓を検索できるようになり、「c3の位置0はm1、位置1はm2…」
という位置別の取り分けが内積で表現可能になる。

### 順序入れ替え(self-attn→cross-attn)の検証結果と壁の完全な特徴付け (2026-08-16)
処理順をencoder(チャンク内causal self-attn)→decompressor(cross-attn)に入れ替え
(標準Transformerデコーダブロック順)。comp_queryの導出もencoder出力のチャンク末尾
(=チャンク全体のcausal要約)に変更。step/forward等価性維持、全テストパス。

結果: **T=1は0.5295で壁は不変** — クエリ位置盲目性仮説も棄却。

しかし壁の正体を数値的に完全特定:
* **bag-only最適値(4桁の多重集合は完全に分かるが順序が全く分からない場合の理論上限)
  = 0.4121 (厳密計算)。観測された中間桁accuracy = 0.4110 でほぼ完全一致**
* 予測がmemory bag内である率 = 100.0% → **多重集合の情報は完璧に伝わっている**
* 全体: (2×1.0 + 8×0.412)/10 = 0.530 = 全構成が収束した値そのもの
* 全介入(容量/ゲート/ALiBi/層数/kv窓/縮退解消/処理順)が無効だった理由:
  どれも「順序情報が値空間に存在しない」ことを解決していないから

機構的説明: **ALiBiはattentionの重みを歪めるだけで、value空間に位置タグを刻まない**。
圧縮 z = Σ_j softmax_j · W_v e_j は「どの桁が入っているか」(bag)は完全に保持するが、
「どの位置にあったか」はsoftmax重みの微差にしか残らず、デコード側で復元できない。
bagが100%伝わり順序が0%という観測はこの機構と正確に整合する。

修正案(本命): **チャンク内位置埋め込み(chunk_size個, d_model)をチャンク化直後の
トークンに加算する**。値空間に位置タグが入り、圧縮ベクトル内で「位置jの桁」が
線形分離可能になる。チャンク内C個のみ・全レベル共有・長さ非依存なので
無限生成の設計目標と両立(レベル別pos_embとは異なる)。copy実験開始時点の
アーキテクチャにはこの種の加算型位置埋め込みが存在しなかった。

### 【ブレークスルー】チャンク内位置埋め込みで壁を突破 (2026-08-16)
`chunk_pos_emb (chunk_size, d_model)` (学習可能、std=0.02初期化)をチャンク化直後の
トークンに加算(全レベル共有・長さ非依存・無限生成と両立):

* **T=1: 1500ステップで token/string ともに100%** (壁0.53を完全突破)
* T~U[1,16]・全位置loss(以前6000stepチャンス張り付きだった設定): 同6000stepで
  tok 0.97 / str 0.79、訓練域内T=4/8でstr 0.83-0.84、T=16で0.52(上昇中)
* bag-order理論の完全な検証: ALiBi(重み側の位置)だけでは圧縮の値空間に順序が
  刻まれない。値空間への位置タグ(chunk_pos_emb)が欠けていた唯一のピースだった
* 訓練域外(T=32/64)への外挿はこのスケール(d=128, 6k step)ではまだ0 —
  次のマイルストーンは本実験(d=512, 50k step, T~U[1,2028])での長さ汎化測定

これまでの改善の総括(全てコミット済みで本体に残る):
1. gate開放初期化 — 信号伝播改善(微益)
2. ALiBi(encoder/compressor/decompressor) — 重み側の位置情報・長さ非依存
3. retrieve_size kv窓 — 過去kチャンクの個別参照(検索構造)
4. クエリ別バイアス — compress_size>1の縮退解消
5. self-attn→cross-attn順序 — クエリの位置分化(標準デコーダ順)
6. chunk_pos_emb — 値空間の位置タグ(決定打)

### ALiBi無効化ablationの結果 (2026-08-17)
「ALiBiの負バイアスは位置情報の恩恵より過去valueの希釈の弊害が大きいのでは」の仮説を
`--no-alibi`(全スロープ0、encoderのcausalマスクは維持)で検証。d512本実験same設定で比較:

| T (訓練max 2028) | with-ALiBi (final ckpt) | no-ALiBi (best ckpt) |
|---|---|---|
| 域内 ≤2028 | str 0.81-1.00 | **str ≈1.00** |
| 2048-6144 (〜3倍) | str 0.76-0.83 | **str 0.98-1.00** |
| 8192-24576 (4-12倍) | str 0.57-0.59 | **str 0.81-0.94** |
| 32768 (16倍) | **str 0.25 / tok 0.86** | str 0.02 / tok 0.69 |
| 49152-98304 | **str 0.19-0.23 / tok 0.82-0.84** | str ≈0 / tok 0.40-0.43 |
| 131072 (64倍) | tok 0.48 | tok 0.20 |

* **T≤25k(訓練域12倍)まではno-ALiBiが圧勝** — chunk_pos_emb導入後のALiBiは位置情報として
  冗長で、value希釈の弊害が支配的(仮説を支持)
* **T≥32kではwith-ALiBiが優位** — recencyバイアスが超長距離での緩やかな劣化(graceful
  degradation)をもたらす。no-ALiBiは25k付近まで高精度だが32kで崖
* 注意: with-ALiBiはbest保存導入前のrunのためfinal checkpoint(軽微に谷側)。
  域内の差の一部はこの条件差の可能性があるが、域外の大差はそれでは説明できない
* d128スモークでは両者同等(T=1立ち上がりはALiBi有りが僅かに速い)

### retrieve_size=1 ablationの結果 (2026-08-17)
retrieve_size(k)>1の寄与を確認するため、k=1(直前チャンク圧縮1点のみ=kv拡張前の構造+
chunk_pos_emb)でd128スモークを実施:

* **T=1固定: str 0.997** — k=1でも解ける(必要な情報は再帰レベル経由で届く。
  chunk_pos_embさえあればkv窓なしでも極短距離コピーは可能)
* **T~logU[1,256]: 8000ステップ完全チャンス張り付き(tok 0.125)** — k=4では同条件で
  tok 0.72/str 0.29だったのに対し、k=1では学習が全く立ち上がらない

解釈: k>1の寄与は「表現能力」(T=1が解けることから、情報経路自体はk=1でも存在)
というより**「最適化の足場」**が大きい。複数Tの混合分布では、k=1だと過去の個別
チャンクへの直接参照ができず「answer位置で一様分布」の局所解から脱出できない。
ALiBiと対照的に、retrieve_size窓は決定的に寄与している。

### retrieve_size=8 スケーリングの結果 (2026-08-17)
d512本実験(k=8, grad-accum 2で実効バッチ64維持, best ckpt)とk=4の比較:

| T (訓練max 2028) | k=4 | k=8 |
|---|---|---|
| 3072-8192 | **str 0.94-0.99** | str 0.64-0.72 |
| 16384-24576 | **str 0.81-0.86** | str 0.48-0.52 |
| 32768 | str 0.02 / tok 0.69 | **str 0.43 / tok 0.93** |
| 49152-98304 | str ≈0 / tok 0.40-0.43 | **str 0.31-0.43 / tok 0.90** |
| 131072 (64倍) | str 0 / tok 0.20 | **str 0.29 / tok 0.89** |

* **k=8では「32kの崖」が消滅**: テスト全範囲(訓練域64倍のT=131072まで)で
  token acc 0.89+を維持し、なだらかな劣化のみ。外挿の到達距離は劇的にスケール
* 引き換えに近中距離(3k-25k)の精度はk=4より低い(str 0.65前後 vs 0.9前後) —
  窓が広いほど「全域で均等に緩やか」、狭いほど「窓の届く範囲で高精度→崖」という
  明確なトレードオフ形状
* d128スモークでも学習の立ち上がりはk単調(k=1:不成立, k=4:str 0.29, k=8:str 0.53)

### 実行時の技術的知見 (2026-08-17)
* k=8はd512訓練でOOM → train.pyに--grad-accum追加(同一Tのマイクロバッチ蓄積で
  実効バッチの意味を維持)
* 大きいTの一発forward評価はsdpaのバッチ次元制限に抵触:
  **FlashAttentionカーネルはバッチ次元>65535で"CUDA error: invalid configuration
  argument"**(efficient/mathは無制限)。ALiBi時代はfloat biasがefficientを選ばせて
  いたため潜在化しており、ALiBi削除で顕在化。MultiHeadAttentionにバッチ次元>65535
  でefficient/mathへフォールバックする分岐を追加。quick_evalもチャンク分割化

### chunk_pos_emb → CausalConv 置き換えの検証結果 (2026-08-18)
長文生成での話題執着(text/oil.txt)の原因仮説「固定加算の位置埋め込みが全レベル・
全層で残差ストリームに蓄積し内容識別を退化させる」を受け、chunk_pos_embを
**RecursiveCompressorConv**(depthwise causal conv, 幅=chunk_size, pre-norm残差+SiLU,
FFN層と同じper-level適用、チャンク境界をまたぐ系列conv)に置き換え。内容依存の
局所混合で順序を値空間に書き込むため、入力非依存の固定成分が蓄積しない
(Mamba/H3/RWKVのshort convと同役割)。

実装の要点:
* conv履歴(直近k-1個のnormed入力)をレベルごとにキャッシュしhiddenで運ぶ。
  hiddenは(conv_caches, attention_hidden)の複合に
* **踏んだバグ2件**: (1)`_apply`はnn.Moduleの予約メソッド(.to()/.double()が使う)で
  オーバーライド禁止 → `_conv_seq`に改名。(2)再帰深度は呼び出しごとに変動する
  (predictでは深いレベルが時々しか活性化しない)ため、xsリストが短い呼び出しで
  キャッシュをzip切り捨て/None破壊していた → 「xsに無い/Noneのスロットは
  旧キャッシュ保持」に修正。fp64でpredict/step/forwardが機械精度(1e-16)一致を確認

結果(d128スモーク、pos_emb版との同条件比較):

| タスク | pos_emb版 | conv版 |
|---|---|---|
| copy T=1 (3k step) | 100% @1500 | 100% @2500 (やや遅いが到達) |
| copy T≤16 all-loss (6k) | tok 0.97 / str 0.79 | **tok 0.996 / str 0.967** |
| selective T≤64 (8k) | tok 0.77 / str 0.39 (sink+pos) | **tok 0.92 / str 0.62** |

* **全タスクでpos_emb版を上回る**。特にselectiveでstr 0.39→0.62と劇的改善 —
  convは位置情報に加えて局所n-gram特徴(データ桁の検出等)も提供するため
* 「同一トークン連続領域ではconvが位置を作れない」という理論的懸念は実験で否定
  (出力チャンクの識別はouter窓の中身の差+conv非一様性+encoder受容野差で賄える)
* 執着現象への効果(固定成分蓄積の解消)は本番LM訓練での長文生成確認が必要

### CausalConv版 d512本実験の結果 (2026-08-18, best ckpt 16600)
現アーキ一式(conv+sink+デコーダ順+ゲート開放、pos_emb/ALiBiなし)、k=4・50k step:

| T | pos_emb k4 (noalibi) | pos_emb k8 | **conv+sink k4** |
|---|---|---|---|
| ≤2048 | str 1.00 | str 0.98-1.00 | str 0.99-1.00 |
| 4096 | 0.998 / 0.984 | 0.960 / 0.641 | 0.997 / 0.973 |
| 8192 | 0.993 / 0.938 | 0.961 / 0.648 | 0.985 / 0.855 |
| 24576 | 0.977 / 0.809 | 0.939 / 0.516 | **0.984 / 0.844** |
| 32768 | 0.692 / 0.020 | 0.926 / 0.430 | **0.850 / 0.195** |
| 49152-98304 | tok 0.40-0.43 | tok 0.90 | **tok 0.80-0.82 / str 0.16-0.18** |
| 131072 | tok 0.20 | tok 0.89 | tok 0.27 |

* **pos_emb k4の「32kの崖」が98kまで後退**: 近距離精度(〜25kでstr 0.83-0.84)を
  ほぼ維持したまま、49k-98kでtok 0.80/str 0.16-0.18を確保。k4のまま
  「k4の近距離+k8寄りの遠距離」といういいとこ取りに近いプロファイル
* conv+sinkの複合変更のため単独寄与は分解していないが、固定位置成分の除去が
  距離依存の表現異方性を減らした可能性
* 131072には依然崖が残る(k8はそこもtok 0.89で粘る — 窓幅の効果は別軸で健在)

### CausalConv版 Copying本実験の結果 (2026-08-19, d512-logu-conv, best ckpt 16600)
現アーキ一式(conv+sink+デコーダ順+ゲート開放、pos_emb/ALiBiなし)、k=4・50k step:

| T | pos_emb k4 (noalibi) | pos_emb+ALiBi k8 | conv+sink k4 |
|---|---|---|---|
| ≤2048 (域内) | str ≈1.0 | str 0.98-1.0 | str ≈1.0 |
| 4096-24576 | str 0.81-0.98 | str 0.48-0.65 | str 0.83-0.97 |
| 32768 | str 0.02 / tok 0.69 | str 0.43 / tok 0.93 | **str 0.20 / tok 0.85** |
| 49152-98304 | str ≈0 / tok 0.40-0.43 | str 0.29-0.43 / tok 0.90 | **str 0.16-0.18 / tok 0.80-0.82** |
| 131072 | tok 0.20 | tok 0.89 | tok 0.27 |

* **域内〜25倍はpos_emb k4と同等を維持したまま、「32kの崖」が98kまで後退**
  (tok 0.8前後を49k-98kで維持)。k4の近距離精度とk8の遠距離耐性の中間的な
  いいとこ取りプロファイル
* 131072では崖(k8のみが到達できる領域は残る)
* conv+sinkの複合変更のため寄与の分解は未実施
* 固定位置成分の除去が距離依存の劣化も緩和した形 — 執着対策として入れた変更が
  長さ外挿にも正の効果

### 層数の検証結果 (2026-08-16)
「2層では記憶/読み出しの分業に障害があるのでは」の仮説を確認するため、T=1同一条件
(d=128, answer loss, lr 1e-3, 3000step)で層数だけ変更:
* 2層: 0.528 / 3層: 0.530 / 4層: 0.530 — **完全に同じ壁、仮説は棄却**
* 全構成(ベースライン/ゲート開放/ALiBi/3層/4層)が0.52-0.53に収束する事実は、
  層数・容量・ゲート・位置バイアスのいずれでもない**情報経路そのものの構造的
  ボトルネック**を強く示唆。デコンプレッサのkv=圧縮1点の粒度問題(チャンク境界と
  桁窓の非整合)が残る本命仮説

### gate初期バイアス開放の検証結果 (2026-08-16)
gate_linear.biasを+4.0(sigmoid≈0.98)で初期化してゲートを開いた状態から学習:
* 感度(先頭10桁→末尾logit): 2〜3倍改善したが**階層ごと≈1/10の指数減衰自体は残存**
  → sigmoid×0.5は減衰の主因ではない。attention加重平均による希釈/残差での相対振幅低下が本体か
* T=1学習: 0.516→0.530(+1.4pt)、loss 1.03→0.91。壁は破れず
* 消去法の現状: 容量(compress_size)✗、ゲート減衰✗ → **位置情報/順序保持が本命仮説**
* 副次的発見: HF `post_init()`の汎用`_init_weights`が全Linearを再初期化(biasゼロ化)するため、
  submodule `__init__`内の手動初期化は無効だった。`RecursiveCompressorLM._init_weights`の
  オーバーライドで対応(変更自体は微益方向なので本体に採用)
### LogKV版 Copying本実験の結果 (2026-08-27, logkv-d512-logu, best ckpt 48200)
`logkv` ブランチの新アーキテクチャ LogKVLM（[doc/logkv.md](../logkv.md)）で同一プロトコル
（d512・2層・8ヘッド・d_ff 1024・chunk 4、50k step、batch 64、lr 3e-4、T~loguniform[1,2028]）。
`exp/copying/train.py --arch logkv`。パラメータは 5.26M（旧 conv+sink 版は 11M）。
位置埋め込み・CausalConv・sink・retrieve窓など**位置/検索の補助機構は一切なし**
（レベル減衰バイアス `i·log(1/C)` のみ）。

| T | conv+sink k4 (11M) | **LogKV (5.26M)** |
|---|---|---|
| ≤2028 (域内) | str ≈1.00 | str 0.86–0.98 / tok 0.98–0.99（T=1 のみ str 0.13、後述） |
| 3k–16k | str 0.83–0.99 / tok 0.98–1.00 | str 0.73–0.89 / tok 0.97–0.99 |
| 24576 | str 0.84 / tok 0.98 | str 0.30 / tok 0.87 |
| 32k–65k | str 0.17–0.20 / tok 0.82–0.85 | **str 0.28–0.42 / tok 0.87–0.91** |
| 98304 | str 0.16 / tok 0.80 | str 0.02 / tok 0.65 |
| 131072 | str 0.00 / **tok 0.27** | str 0.01 / **tok 0.67** |

* **位置情報ゼロ・半分のパラメータで域内 str 0.86–0.98、8倍外挿 (16k) まで str 0.73–0.89**。
  階層窓の因果構造（c<j 自ブロック / c≥j 前ブロック）だけから順序を復元できている
* **崖がない**: 旧アーキは 32k で str が崩落し 131k で tok 0.27（bag 最適 0.41 未満 = 順序も
  内容も喪失）。LogKV は 131k（64倍）でも tok 0.67 と bag 最適 0.41 を上回り、部分的に
  順序を保持。32k–65k 帯では旧アーキを上回る
* 一方 24k 以降の plateau は低い（str ≈0.3）。レベル減衰の log d ロジット逆風、または
  メモリ10桁が 16k トークンの圧縮サブユニット 1 本に埋まる粒度の問題が候補。
  T=65536（=4^8）で str 0.42 に回復するなど C の累乗境界に依存する非単調性があり、
  レベル構造由来
* **T=1 の異常** (str 0.13): 答え位置 7・8 だけが隣接桁と取り違えられる
  （例: 正解 …8,3,1,6 → 予測 …8,1,1,6。位置別正答率 1.00×7, 0.65, 0.46, 1.00）。
  T≥2 では同じ位置が 1.00。桁列とマーカー列の間に blank が 1 つもない T=1 特有の
  「今答えの何桁目か」の推定ずれ（位置符号化がなく blank 数から相対位置を推定している）
  と考えられる。訓練分布では T=1 は約 9% を占めるのに残る点は要調査
* best ckpt は step 48200（旧アーキは 16600）で 50k 時点でも改善中 — 訓練延長の余地あり
* 所要時間: 50k step 約 2.3 時間（1 GPU）

### LogKV + 学習可能位相埋め込み版の結果 (2026-08-28, logkv-d512-logu-phase, best ckpt 44700) — 全域で完全解
T=1異常の原因(同一トークン連続区間での位置縮退、[doc/logkv.md](../logkv.md) §6.2)への対策として、
絶対位置のC進数各桁(=レベルごとのブロック内位相)で選ぶ学習可能ベクトルの和をattention入力に
加算する位相埋め込み(`--phase-emb`、+65k params)を追加。他は `logkv-d512-logu` と同一条件。

| T | LogKV (位相なし) | **LogKV + 位相** |
|---|---|---|
| 1 | str 0.13 | **1.000** |
| 2–2048 (域内) | str 0.86–0.98 | **1.000** |
| 3k–16k | str 0.73–0.89 | **1.000** |
| 24k–65k | str 0.28–0.42 | **1.000** |
| 98k–131k (64x) | str 0.01–0.02 / tok 0.65–0.67 | **1.000** |
| 262144 (129x) | — | **1.000** (n=64) |
| 524288 (259x) | — | **1.000** (n=64) |
| 1048576 (517x) | — | **1.000** (n=32) |

* **T=1〜1,048,576の全ホライズンでtoken/string accuracy 1.000**。EMA loss 2.4e-9。
  T=1の位置別正答率も全位置1.00(縮退の解消を直接確認)。旧アーキ最良(conv+sink)の
  「32kで崖・131kでtok 0.27」、位相なしLogKVの「24kからplateau」をともに完全に解消
* 機構の解釈: (1) 桁が置かれる絶対位置0–9の位相タグはTによらず固定なので、桁のvalueが
  「(桁, 位置タグ)の束縛表現」として圧縮を通過し、上位レベルの1本のベクトル(T=1Mでは
  約100万トークンの要約)に10組が重ね合わせで保存される (2) 答え位置ではマーカー
  連続区間内の「何個目か」が位相で数えられるようになり、対応する位置タグで復号する。
  圧縮のattentionプーリングがblankを捨てて桁を残す学習と組み合わさり、任意長で無希釈
* 位相埋め込みは有界・周期的(桁ごとにC本のベクトルを巡回)なので、絶対位置埋め込みで
  ありながら長さ外挿を阻害しない。`phase_levels=16`で4^16≈43億トークンまで定義
* 訓練は45k付近で一時不安定(quick_eval T=4056で0.03)だがbest ckpt(44700)は完全解、
  50kのquick_evalも全T 1.0
* 位相なしとの比較で、LogKVの遠距離plateauの主因は「レベル減衰のlog d逆風」ではなく
  「同一トークン連続区間での位置縮退(答え位置のカウント不能)+順序符号の欠如」だったと
  結論できる。レベル減衰は1MトークンでもT+10距離の検索を妨げていない

### 位相レベル数の制限 (2026-08-28, logkv-d512-logu-phase2, phase_levels=2 = 周期16)
Selectiveで判明した「未学習の高位桁ベクトルによる域外崩壊」対策として位相を周期16に制限。
Copyingは桁が位置0–9固定でタグが周期16でも一意なので影響なしの予想どおり、
**T=1〜131072の全ホライズンで string accuracy 1.000 を維持**。best ckptは23100で
phase_levels=16版(44700)より収束が速い(未使用の高位ベクトルが無い分、学習が素直)。

### 学習可能減衰係数 (2026-08-28, logkv-d512-logu-phase2-ldecay, best ckpt 19800)
レベル減衰の係数 β(初期値 log C=1.386)をヘッド・層ごとに学習可能化(`--learnable-decay`)。
他は phase2 固定版と同条件。

| T | phase2 固定 | phase2 学習減衰 |
|---|---|---|
| 1–65536 | 1.000 | 1.000 |
| 98304 | 1.000 | 0.988 |
| 131072 | 1.000 | 0.996 |

学習された β: layer0 = 1.51–2.05(全ヘッドが log C より大、強い recency)、
layer1 = 1.23–1.54(log C 前後)。**β は 0 に向かわず log C 近傍に留まる** — データは減衰を
不要とは判断しない。第1層が集約(減衰強)、第2層が検索(減衰弱め)という分業。収束は速い
(best 19800 vs 23100)が、訓練域を大きく超える 48–64倍で僅かに崩れる(学習係数は訓練で見た
距離範囲に最適化される。厳密な外挿は固定 log C が上)。

### Gated attention (2026-08-29, logkv-d512-logu-phase2-gated, best ckpt 44700)
recursive_compressor.MultiHeadAttention と同形のゲート(sigmoid(W_g x+b) を各ヘッドの attention
出力に要素ごとに乗算、`--gated-attention`、+0.5M params)を phase2 固定版に追加。
**T=1〜131072 の全ホライズンで string 1.000 を維持**(収束は 44700 と固定版 23100 より遅い)。
ゲートは bias≈0.5 のままだが内容依存でほぼ二値的(第1層: <0.1 が 53%、>0.9 が 28%)に働き、
答え(マーカー)位置では平均 0.15 と attention 出力を強く絞る次元選択器になっている。
