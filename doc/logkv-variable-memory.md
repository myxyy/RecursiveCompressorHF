# 標準CausalConv：可変桁Copying / Selective Copying

2026-09-14、固定10桁Copyingの16M評価成功を受け、ユーザー承認により
`logkv-causal-conv`をmainへfast-forwardマージした（標準化commit `654f311`）。
新規訓練CLIはConv幅4・位相埋め込みなし・gate/self slotありを既定とする。
旧checkpointを読む低水準configの既定値は互換性のため維持する。
モデルテスト159件、CLIの標準設定・無効化オプションの確認を通過した。

続くユーザー依頼は、[以前の可変桁実験](logkv-position-study.md)と同条件で
新しい標準構成を学習・評価すること。今回新しく学習する構成はCausalConvのみで、
CopyingとSelectiveを別モデル・別GPUとして並列に実施した。

**状態：2026-09-14 19:12:40 JSTに学習・全880セルの評価・自動監査が完了。GPU解放済み。**
本学習は14:39:11 JST開始。実測は本実行4.56時間、事前確認を含め4.67時間。
両タスクの最初の100 stepsは事前benchmarkと指標がbit一致し、初期重みのハッシュも一致した。
[開始確認](experiments/logkv-variable-memory-20260914/launch.json)。準備commit `e7c4004`。
終了見込み19:40 JSTより早く、各50k学習後のbest/final計880セルとCPU監査まで完了した。

見積もりは事前確認込み約5.08時間。最大形状の使用メモリ8.98 GiB、300 stepsはCopying64.18秒・Selective64.25秒。
220セル×16例の評価は109.58秒・108.27秒。学習50k、25回の検証、best/final各256例へ換算し、
25%の余裕と5分の予備時間を加えた。停止期限は2026-09-14 22:02:35 JST。
[benchmark記録](experiments/logkv-variable-memory-20260914/preflight.json)、
[評価データのCPU照合](experiments/logkv-variable-memory-20260914/evaluation_smoke.json)。

## 完了結果（2026-09-14）

**可変桁学習でも10桁Copyingの長距離保持は大きく改善したが、16桁以上の完全復元は未解決。**
Copyingのbestは50,000 stepでfinalと同一の重み・同一の保存評価データ。
独立した2モデルの成功ではない。Selectiveのbestは48,000 stepでfinalと異なる。
bestは検証集合全体から選び、各評価セルで選び直してはいない。

### Copying

以下はP=0、各256例。best/final共通。

| M | T=64 完全一致 | 桁精度 | T=131,072 完全一致 | 桁精度 |
|---:|---:|---:|---:|---:|
| 10 | 252 / 256 | 99.73% | 238 / 256 | 98.98% |
| 16 | 17 / 256 | 87.70% | 1 / 256 | 72.17% |
| 32 | 0 / 256 | 53.65% | 0 / 256 | 42.44% |
| 64 | 0 / 256 | 35.20% | 0 / 256 | 30.71% |

M10はT131072でも238/256（92.97%）が完全一致。ただし固定10桁だけで学習したモデルのような
全評価点100%には届かず、T1も253/256、T16も252/256だった。
固定10桁の既存checkpointと今回の可変桁checkpointは別物であり、前者の16M・8/8を
後者の成績として扱わない。今回の可変桁checkpointは16M未評価。

M16は短距離でも難しく、T1で10/256、T64で17/256。M32/M64はP=0とprefix評価を合わせた
各53セル、best/finalとも完全一致0/256だった。M32の桁精度はT1で54.85%、T64で53.65%と、
短い待ち時間から低い。長距離にしたときだけ崩れる現象ではない。

### Selective Copying

各セルはbest / final、P=0、各256例。

| M | T=64 完全一致 | 桁精度 | T=131,072 完全一致 | 桁精度 |
|---:|---:|---:|---:|---:|
| 10 | 43 / 37 | 80.98% / 80.62% | 0 / 0 | 59.49% / 53.24% |
| 16 | 0 / 0 | 63.84% / 66.04% | 0 / 0 | 47.07% / 44.31% |
| 32 | 0 / 0 | 47.07% / 46.66% | 0 / 0 | 32.70% / 32.25% |
| 64 | 0 / 0 | 32.79% / 32.64% | 0 / 0 | 22.85% / 24.11% |

M10・T16では159/114例、T256では25/10例、T1024では24/3例が完全一致。
最長距離は全Mで完全一致0/256だが、M10 bestの桁精度は59.49%まで残った。
M16にはT1などで完全一致例があるものの、T64/P0は0/256。M32/M64はprefix評価も含め
各53セル、best/finalとも完全一致0/256。

![可変桁の待ち時間別精度](experiments/logkv-variable-memory-20260914/analysis/horizons.png)

### 過去の可変桁方式との比較

同じ可変桁・同じセルのbestを比較する。以下はM10/P0、各256例。
学習時の厳密な決定論設定に差があるため、1 seedの過去実験との比較として扱う。

| タスク / T | none 完全一致 | phase2 完全一致 | combined-no-decay 完全一致 | CausalConv 完全一致 |
|---|---:|---:|---:|---:|
| copying / 64 | 0 | 254 | 193 | 252 |
| copying / 131,072 | 0 | 1 | 0 | 238 |
| selective-copying / 64 | 0 | 12 | 23 | 43 |
| selective-copying / 131,072 | 0 | 0 | 0 | 0 |

M10 Copyingの長距離での改善は明確だが、T64の完全一致はphase2の254/256に対し252/256で、
全指標を上回るわけではない。Selective M10/T131072の桁精度はnone 21.05%、phase2 16.80%、
combined-no-decay 13.32%に対してCausalConv 59.49%。長距離の部分的な保持は改善した。
全6方式とのセル別比較は[比較データ](experiments/logkv-variable-memory-20260914/results/historical_comparison.json)に収録した。

### Prefixと未学習128桁

Copying M10は追加のP7/15/63でも完全一致249〜256/256（T16/64/256/2048の12セル）。
M16は同じ12セルで15〜45/256、M32/M64は0/256。先頭空白を加えるとM16の一部は改善するが、
完全復元には至らない。Pによって入力配置と評価例も異なるため、単純な位置効果とは断定しない。

未学習M128は両タスクの全8セル、best/finalとも完全一致0/256。
T64/P0の桁精度はCopying 25.73%、Selective best 23.62% / final 23.05%。
学習範囲外の桁数への完全一致外挿は得られなかった。

### 桁別の傾向と解釈

T64/P0のCopyingでは、M16/32/64でも先頭4桁は全例正解。
M16の第15桁は195/256誤答、M32/M64でも10桁を超えたあたりから誤答が増える。
Selectiveも後半ほど精度が下がる傾向がある。

![桁位置ごとの精度](experiments/logkv-variable-memory-20260914/analysis/digits.png)

今回の条件では、長い待ち時間に耐えることと、長い記憶列を復元することの両方を
同程度には達成できていない。小さい記憶列の保持にはCausalConvが有効だった一方、
16桁以上は短距離から誤るため、圧縮・読み出し・学習のどこが後半の復元を制限するかが残る。
可変M同時学習の干渉、訓練量、モデル容量、読み出しの位置識別を分離した実験ではなく、
原理的な容量上限を示した結果ではない。固定M16/32の独立学習と比べれば、
可変Mを混ぜた学習による難しさを切り分ける次の候補になるが、今回は実行していない。

### 完了確認

19:12:40 JSTに両50k学習・best/final計880セル・CPU自動監査を完了。
本実行4.56時間、GPU事前確認込み4.67時間で、見積もり5.08時間・停止期限22:02:35 JST以内。
全GPU解放済み。追加学習・評価は実行もキューもしていない。
完了後にも33ソース、実行スクリプト、checkpoint、元監査の全結果ファイル、880個のNPZをハッシュ照合し、
保存予測から正答数と桁別誤答を再計算した。元の監査は入力seedと配置の再生、
全50kのM出現数、best選択、最初の300 stepsのbenchmark一致まで確認している。

- [全880セル](experiments/logkv-variable-memory-20260914/results/metrics.json)
- [訓練とbest情報](experiments/logkv-variable-memory-20260914/results/training.json)
- [完了後の確認](experiments/logkv-variable-memory-20260914/analysis/review.json)
- [解析コード](experiments/logkv-variable-memory-20260914/analyze_completed.py)

## 条件

| 項目 | 設定 |
|---|---|
| モデル | 重複なしLogKV、2層、d512/H8/ff1024/C4 |
| 標準構成 | 各Block入力に幅4のConv残差、phaseなし、gate/self slotあり、固定−i log C |
| 追加方式 | RoPE、相対K/V、圧縮位置変換、KV/V norm、増幅は使用しない |
| パラメータ数 | 5,792,256 |
| 学習桁数M | {10,16,32,64}から一様選択 |
| 待ち時間T | loguniform[1,2028] |
| 先頭空白P | 整数[0,63]から一様選択 |
| 系列長 | P+T+2M、最大2,219 |
| 学習 | 各50k steps、実効batch64、microbatch32×2回蓄積、microbatch間でM/T/P共有 |
| 最適化 | AdamW lr3e-4、warmup1000、weight decay0、clip1 |
| 損失 | 全位置のposition-aligned CE、正解を入力へ戻さない |
| seed | 初期化0、内容生成1、M/T/P生成2、検証54321、最終評価12345 |
| 学習・評価精度 | fp32重み＋bf16 autocast（以前の可変桁実験と同じ） |
| best選択 | 2000 stepsごとの独立検証集合、macro完全一致率→macro桁精度→−訓練EMA |

タスク生成器は以前の`24b360c`からバイト単位で同一。訓練・検証・評価の手順と
乱数消費順を維持し、初期化のみ標準CausalConvへ変更した。旧モデルと共通する全重みの
初期値は以前と同一（SHA256 `2898927c4fec087b5f0d10ab5886d8bc40184c79bdff06bed551da9fe73369b5`）。
追加Convパラメータだけをseed0で新規初期化し、両タスクで同一の未学習モデルから始める。
固定10桁の学習済み重みからの転移ではない。

以前は厳密な決定論モードを無効にした実行を含むが、今回は有効にする。
旧6方式の完了済み結果と比較し、旧方式は再学習しない。共通初期値・データ条件は揃えるが、
決定論モードの差と各1 seedという限界を含む過去実験との比較として扱う。

固定10桁実験とはM/P以外にも、勾配蓄積、best選択、評価時の重み精度が異なる。
精度差を記憶桁数だけの効果とは断定しない。

## 評価

best選択用の検証はM4種×T{16,64,256,1024}×P{0,7}、各32例。
最終評価とはseedを分離する。最終評価はbest/finalそれぞれ以下の220セル・各256例。

- M4種×T=1〜131,072の従来41点、P=0：164セル
- M4種×T{16,64,256,2048}×P{7,15,63}：48セル
- 未学習M128×T{16,64,256,2048}×P{0,15}：8セル

合計2タスク×2checkpoint×220＝880セル。M64は学習範囲内、M128は桁数の外挿である。
P7/15/63は学習範囲内。chunk4096で全回答部分を回収し、回答がchunk境界をまたいでも採点する。
セル別seedをtask/M/T/Pから決めるため、評価順序は入力を変えない。
各例の記憶列、実配置、予測、marginをRAID上の圧縮NPZへ保存し、完了時にCPUで
元生成器から再生して照合する。リポジトリには指標・ログ・桁別誤答・NPZハッシュを収録する。

## 実行・再現

- 実験API：`exp/variable_memory/`、commit `90f13b1`。
- [実行コード・記録](experiments/logkv-variable-memory-20260914/README.md)
- [凍結33ファイルのハッシュ](experiments/logkv-variable-memory-20260914/source_manifest.json)
- 保存先：`/mnt/raid0/RecursiveCompressor/experiments/logkv-variable-memory-20260914/`
- 可変タスク・固定10桁タスクとの一致・回答chunk跨ぎ・評価seedの14 testsを通過。
- GPU0=Copying、GPU1=Selective。事前最大M64/P63/T2028のmicrobatch32×2更新後、
  本物の訓練300 stepsと220セル×16例の評価を両GPUで測定する。
- 8時間以上の見積もりなら本学習前に確認。実行上限は事前GPU確認開始から7.5時間。
  失敗時は両workerを停止し、自動再試行や次のcampaignは行わない。
- 追加seed、別幅・層数、可変桁16M評価は今回の範囲に含めない。
