# CLAUDE.md

## Project Overview
Python ML project: a language model with a custom hierarchical-kv-compression architecture (**LogKV**, the current main line). The previous recursive-compression architecture (RecursiveCompressor) is retained as legacy. Uses HuggingFace (PreTrainedModel), PyTorch DDP, and uv for package management.

## Merge constraint
- User instruction (2026-09-08): do not merge `logkv-position-study` into `main` until the selected configuration achieves exact Copying at T=16,777,216. The historical fixed-M=10 phase2 result does not satisfy this gate for a replacement positional scheme. Prior documentation-only integration into local main did not include experimental model code. Keep subsequent changes on the experimental branch while this gate is unmet.

## Experiment scheduling and power constraints
- User instruction (2026-09-08): sustained use of all 6 GPUs conflicts with household power needs. Earlier authorization to parallelize experiments is subject to the following constraints.
- Before starting an experiment or batch expected to take 8 hours or longer, report the estimated duration and GPU usage and obtain confirmation. Count the whole proposed batch, not just individual runs.
- Updated user instruction (2026-09-08): continuous experiments are authorized when using at most 2 GPUs. This supersedes the earlier requirement to ask for continuation at every stage boundary for such runs. Keep meaningful task/result boundaries, but do not ask again solely because a stage ends within this GPU limit.
- Copying and Selective Copying remain separate training/checkpoint/evaluation tasks. The user has preauthorized continuing the current local-RoPE Copying comparison into the planned Selective comparison, at most 2 GPUs in total. The two stages together are expected to take about 12–14 hours; continuity is authorized, while any individual planned experiment stage/batch newly expected to require 8 hours or longer still needs duration confirmation. Do not silently enlarge a batch or add new ablations.
- For the current campaign, audit Copying results before starting Selective, re-estimate the whole three-mode Selective stage from observed timing, and stop for confirmation if that estimate reaches 8 hours. Preserve each stage's 8-hour runtime limit. Stop on a failed audit or failed stage; no automatic retries, 16M extension, or main merge.
- Latest follow-up (2026-09-09): user requested RoPE without phase2. Run retrieval-only and compressor-only RoPE separately for each of Copying and Selective, using the same frozen `afaea2b` implementation and 50k fixed-M10 protocol. This new four-run campaign has a stricter **8-hour total limit**, estimated about 7h45m from prior matched timings. At most two GPUs; finish/audit Copying before Selective, and pause before Selective if projected total duration reaches 8h. No phase embedding, fixed level decay retained. Report: `doc/logkv-rope-only.md`; artifacts and runner: `doc/experiments/logkv-rope-only-20260909/`. Do not enlarge this campaign or automatically extend to 16M. Phase removal changes shared initialization (recorded in preflight), so do not describe its historical comparison as identical-initial-weight ablation.
- Copying follow-up reviewed 2026-09-10: both no-phase modes completed 50k steps and all 132 evaluation cells. Retrieval RoPE best is 98.4375–100% exact across 33 horizons (T8192 254/256); final T8192 250/256 but T3 only 10/256. Compressor RoPE best/final T8192 are both 0/256. Full comparison and caveats are in the no-phase report. Selective started at 01:05:14 JST after successful Copying audit; projected whole-campaign duration 7.66h. As of this review it is running on GPUs 0/1; retain the whole-campaign stop deadline 2026-09-10 05:16:28 JST. Do not stop it merely because Copying has been reported, and do not start extra experiments.
- Explicit exception (2026-09-10): user authorized one additional GPU specifically to extend the no-phase retrieval-RoPE Copying evaluation to T131072. Use GPU 2 for best/final, n256, bf16, same 41-point grid; keep ongoing Selective on GPUs 0/1. This permits at most three simultaneous GPUs for this extension only, not a general increase in the training limit. Isolate extension outputs under `logkv-rope-only-20260909/extension-131072`, verify checkpoints and overlapping horizons, and release GPU 2 when complete. No 16M evaluation or main merge authorized by this extension.
- T131072 extension completed/reviewed 2026-09-10: GPU 2 ran 01:19:16–01:47:22 JST and is released. Both checkpoints at T131072 have 247/256 exact strings and 2551/2560 correct digits (96.48% / 99.65%). Best falls to 89/256 at T49152, then recovers to 254/256 at T65536; final gives 251/256 and 232/256 at those T. All 82 cells audited, prior 33 horizons per checkpoint reproduce exactly, checkpoint hashes unchanged. Do not claim uniform long-range exactness or 16M success. See the extension section of `doc/logkv-rope-only.md`. Selective training continues on GPUs 0/1 under its original deadline; no further GPU-2 work is queued.

- No-phase Selective reviewed 2026-09-10: both 50k-step runs and 132 evaluation cells completed; entire four-run campaign finished 04:52:48 JST in 7.606h, within 8h. All GPUs released, no further experiment queued. Retrieval-only T64 exact best/final is 53/256 / 31/256 (20.70% / 12.11%); T1024 both 5/256, T2048 both 2/256, T4096 and T8192 both zero. Compressor-only T64 and longer table points are zero in both checkpoints. Phase removal does not show consistent Selective improvement; one seed and changed shared initialization limit causal claims. All logs/results, checkpoint hashes, source archives and phase-on references verified. Report: `doc/logkv-rope-only.md`; review: `doc/experiments/logkv-rope-only-20260909/selective-copying/review.json`. This completion supersedes earlier running-Selective status notes; keep the main merge gate.

- New authorized self-slot ablation (2026-09-10): user approved training/evaluation and the proposed Copying diagnosis. Run one no-phase retrieval-RoPE model with self_slot=false, same frozen afaea2b implementation and reconstructed initial weights/data as the existing self-on baseline. Fixed M10, 50k steps, best/final 41 horizons through T131072 n256. Include paired n16 memory/boundary/attention probes and IEEE fp32 checks, plus replay of the baseline T131072 errors. GPU 0 only, estimated 5-6h total, hard limit 7.5h. No Selective, additional seeds, 16M, or main merge. See `doc/logkv-self-slot.md` and `doc/experiments/logkv-self-slot-20260910/`. Runner stops on failure/deadline; do not restart automatically.

- Self-slot ablation reviewed 2026-09-10: completed 10:30:06-14:35:35 JST in 4.092h on GPU 0, all GPUs released. No-self best step39100; T131072 exact best/final 175/256 / 2/256 vs self-on 247/256 both. All 82 new eval cells and 48 diagnosis cases audited. Self-on best paired T49152 has 13 digit-9 errors all equal to digit10; T49153 has 11 digit-8 errors all equal to digit9, both absolute position49170. fp32 still has 11 such errors at T49152. Replaying nine T131072 bf16 errors in fp32 fixes 3 best / 0 final; do not infer overall fp32 accuracy from selected errors. Answer-layer2 self mass is near zero at wrong positions; attention overlap is not information retention or a causal measure. Self removal is not supported by this seed; no further jobs or main merge. Report: `doc/logkv-self-slot.md`; artifacts/review: `doc/experiments/logkv-self-slot-20260910/results/`.

- Readout intervention study authorized 2026-09-10: user approved the proposed causal probes and explicitly permits up to 3 GPUs for this verification. Inference only on existing self-on/no-phase retrieval-RoPE best/final; GPU0 best bf16, GPU1 final bf16, GPU2 best IEEE fp32. Discovery16 seed4321, validation32 seed20260911; single-answer head/score/gate/FFN interventions and reciprocal/sham controls. Shared blank prefixes immutable; no training, Selective, 16M, or main merge. Total cap2h, stop all workers on failure, no automatic retries. Plan: `doc/logkv-readout-intervention.md`; scripts: `doc/experiments/logkv-readout-intervention-20260910/`.

- Readout intervention results reviewed 2026-09-10: all350 cases complete on3GPUs, 16:10:24-16:11:00 JST (~36s execution/audit, excluding setup; immutable prefix reuse). AllGPUs released. Best held-out32 T49152 digit9: baseline16/32; layer1head8 donor output, memoryslot RoPE -3->-2, or layer2gate donor each32/32; extra blankslot removal stays16/32 with identical logits. T49153 digit8:17/32->32/32. Reverse head8 patch at goodT32768:32/32->15/32. Best fp32 replicates rescue; final targetdigits already correct, othererrors persist. Supports a position-sensitive layer1head8/gate readout path, not a deployed fix or full mediation proof. No model changes, retraining, 16M, or main merge. Report `doc/logkv-readout-intervention.md`; all raw/compact artifacts and review under its experiment directory.

- RoPE angle-scale comparison authorized 2026-09-10: user requests fixed model size and permits up to 3 GPUs for this campaign. Train Copying only at alpha=1, 2/3, 1/2 with matched initialization/data, no phase2, retrieval RoPE only, self slot on, fixed M10, 50k steps. GPUs0/1/2; estimated 5-6h total, hard cap7.5h. Evaluate best/final standard41 horizons through T131072 n256 plus fixed34 paired horizons n32. Stop all workers on failure/deadline, no retries or follow-on tasks. Report `doc/logkv-rope-scale.md`; runner/artifacts `doc/experiments/logkv-rope-scale-20260910/`. No Selective, extra seeds, 16M or main merge. This supersedes the completed intervention-study GPU scope for the newly approved comparison only.

- RoPE angle-scale comparison reviewed 2026-09-11: all3 runs completed50k and all450 evaluation cells audited; 21:49 Sep10-03:02 Sep11 JST (5.228h), allGPUs released. Alpha2/3 best reaches256/256 atT131072 but212/256 atT65536 and209/256 atT98304; paired32 hasT49146=3/32 andT49147=9/32, with23 digit8 errors all copying digit9. Alpha1/2 best/final T131072=213/104 out of256. New alpha1 baseline is31/33 vs historical247/247: reconstructed initialization/RNG and CPU fullsize legacy/new outputs/gradients match, but training logs already differ atstep100; cause unisolated, no claim of GPU determinism or general alpha superiority. Alpha2/3 final T131072=246/256 andT1=82/256. Results/limits: `doc/logkv-rope-scale.md`; post-run CPU audit `results/manual_review.json`. No follow-on experiments, Selective,16M or main merge.

- End-anchored global RoPE campaign authorized 2026-09-13: user approved the first proposed approach, aligning child Key frames before recursive pooling on the original unbounded token axis. New branch `logkv-aligned-rope`; no main merge. Add `aligned_rope`/`aligned_rope_scale`, keep self slot, phase2 off, fixed M10/model size. Compare local retrieval RoPE scale1 versus aligned scale1 on GPUs0/1,50k steps, best/final standard41x256 through T131072 and paired34x32 (300 cells). Both use deterministic PyTorch kernels and CUBLAS_WORKSPACE_CONFIG=:4096:8; short20-step repeats of each mode agree across GPUs0/1. Benchmark projects about6.23h including margin/evaluation/preflight. Whole GPU batch cap7.5h from preflight start, fail-stop and no retries. Copying only in this batch; no automatic Selective, extra seeds or16M. Full design/protocol: `doc/logkv-aligned-rope.md`; runner/artifacts: `doc/experiments/logkv-aligned-rope-20260913/`. All327 model tests and actual-checkpoint evaluator smoke passed.

- End-anchored Copying reviewed 2026-09-13: both 50k runs and all 300 cells complete, 12:10:43–17:06:08 JST including preflight (4.923h). Independent CPU audit passed all counts, 36 source / 9 script / 40 result hashes and checkpoint shapes. Local best/final T131072=252/213 out of 256; aligned=0/0 with digit accuracy 12.070%/12.813%, each checkpoint outputs one constant 10-digit string across all 256 memories. Aligned best T2048=256, T3072=86, T4096=0; final training interval exact 7.594%. Local paired T49146=6/32 both, 9th-digit errors equal the 10th digit. Report `doc/logkv-aligned-rope.md`, additional audit `results/manual_review.json`. No 16M or main merge.
- New Selective continuation authorized 2026-09-13: user requested checking completed experiments and continuing unperformed work. Copying audit passed; started a separate two-mode Selective stage (local-control/aligned) at 17:19:05 JST, same frozen 8dcfea4 source, saved common initial weights, deterministic setup, fixed M10/model size/50k steps. At most 2 GPUs, standard 33 T through 8192 x 256 x best/final = 132 cells with actual scatter positions/predictions. The 300-step Selective benchmark projects 5.43h including 15% margin, evaluation and preflight; 20-step repeats are bitexact on GPUs 0/1. Whole new stage cap 7.5h including preflight; stop on failure, no retries/extra ablations/16M/main merge. The actual checkpoint evaluator smoke passed for both modes and checkpoints. Hard deadline 2026-09-14 00:44:55 JST; supervisor PID 1818413. This explicit continuation supersedes the previous Copying-only boundary. See `doc/logkv-aligned-selective.md`; scripts `doc/experiments/logkv-aligned-selective-20260913/`.


- End-anchored Selective completed 2026-09-13: both 50k runs and all 132 evaluation cells finished at 21:36:04 JST; 4.283h execution / 4.352h including GPU preflight, within 7.5h. All GPUs are released. Local best/final T64 exact 17/28 out of 256 vs aligned 25/17, so the short-horizon advantage reverses by checkpoint. T8192 exact is zero in both modes/checkpoints; digit accuracy local 46.29%/53.63% vs aligned 19.34%/17.89%. Best steps local 49000 / aligned 48100. Independent CPU audit passed all 132 cells / 337,920 digits, regenerated all memories/scatter positions, and verified source/script/result/checkpoint hashes. Both Copying and Selective comparisons are now complete; no further experiments are queued. See `doc/logkv-aligned-selective.md`; keep frozen executed scripts and original artifacts unchanged, with any additional review in `review.py` and `results/manual_review.json`. One training seed, no 16M test or main merge. This completion supersedes the preceding running-stage/deadline notes.

## Latest fixed-memory experiment
- Completed 2026-09-08: combined-no-decay, fixed M=10, no prefix, original fixed-memory train/eval protocol, 50k steps per task on 2 GPUs. Full report: `doc/logkv-fixed10-combined.md`; artifacts: `doc/experiments/logkv-fixed10-combined-20260908/`. Copying best/final are identical: all 30 standard points through T=3072 exact, T4096 1/256, all evaluated T>=8192 zero exact; quick T1014 still has errors. Selective final T64 46.09%, but all evaluated T>=1024 zero exact for best/final. GPUs released; no follow-up stage or main merge before the required confirmation/merge gate.

- Approved boundary follow-up completed: T=4064..4112, paired n=256, final Copying, one GPU. First error at T4077, exactly when the last answer reaches position 4096. All 44,800 outputs at positions below 4096 in this sweep were correct. Swapping batch sizes at T4076/4077 leaves all predictions identical. See the boundary section/artifacts in the fixed-memory report. GPUs released; no additional stage is queued.

## Latest compression diagnosis (2026-09-08)
- Completed the user-approved fixed-M10 Copying representation probe on GPU 0 only, without training: paired 16 memories plus blank controls, real prefix compression depths 2..8 (N=16..65,536), bf16 and IEEE fp32. Report: `doc/logkv-compression-diagnostic.md`; raw traces/scripts: `doc/experiments/logkv-compression-diagnostic-20260908/`.
- Phase2 stays exact 16/16 at every probed depth in both precisions; combined-no-decay is 0/16 at depths 6..8 in both. At depth 6, fp32 layer-2 memory-minus-blank V has cosine to the previous depth 0.99998 vs 0.53306, while both retain about 93% of their own depth-2 difference norm. Combined layer-1 difference norm falls to 6.92%. Queries and average memory attention mass barely change at the first failure. Supports representation/readout mismatch, but does not isolate U0 causally or prove irreversible information loss. This is not a 16M reevaluation. All GPU work finished; no next stage queued, main merge gate remains unmet.

## Architecture
- Local RoPE study: branch `logkv-phase2-rope` retains the same main merge gate. `retrieval_rope` / `compressor_rope` default false; rotate temporary score keys by local child-position differences with base 10,000, never directly rotate the cached/pooled K/V. Even head dimensions required. Full design and staged experiment status: `doc/logkv-phase2-rope.md`. Copying and Selective Copying are separate three-mode stages. The latest user instruction authorizes continuous execution with at most two GPUs; audit and duration-check between them, without another routine continuation question.
- Local RoPE results reviewed 2026-09-09: all six 50k-step runs and 396 evaluation cells completed (33 horizons through T8192, n256, best/final). Copying is exact in all 198 cells for all three modes. Selective T64 string best/final: phase2 9.38/12.11%, retrieval RoPE 26.95/9.77%, compressor RoPE 23.05/20.70%; both RoPE modes are 0/256 at T8192 in both checkpoints. Short-horizon gains do not establish long-horizon Selective improvement or 16M Copying retention. About 9h22m total, at most two GPUs; all GPUs released, no additional stage queued. Main merge gate remains unmet. Raw artifacts and reviewed interpretation are in the local RoPE report.
### LogKV (main)
- `logkv.py` - Core module. Per level i (sub-unit = C^i tokens), each query attends only to completed sub-units in its current block (c < j); these disjoint intervals partition the entire past. All levels share one softmax (at most C−1 slots per level). See `logkv-refine.drawio.png` and `doc/logkv.md` §6.17. Compression is attention pooling with the chunk-last query. Has `forward`/`step`/`predict` (fp64 machine-precision equivalent) plus `LogKVBlock` (pre-norm attention+FFNSwiGLU) and options: `phase_emb`/`phase_levels`, `gated_attention`, `self_slot`, `learnable_decay`, `kv_norm`, `v_norm_only`, `level_amplify`.
- `logkv_lm.py` - LogKVLM language model (PreTrainedModel + generate; `past_key_values` carries the opaque per-layer hidden list).
- `configuration_logkv.py` - LogKVConfig.
- `train_logkv.py` - **DDP data-parallel** training (the model fits on one GPU). Muon + AdamW, bf16 autocast, control.cmd, `--resume latest` (skips consumed data, absolute `--max-steps`, EMA carry-over), periodic Japanese sample generations to `samples.log`.
- `predict_logkv.py` - Text generation for LogKV checkpoints.
- `exp/copying/`, `exp/selective-copying/` - Copy Memory Problem / Selective Copying suites (`--arch logkv` supported; selective wraps copying via task-module injection).
- `doc/logkv.md` - **The design/experiment record for LogKV. Read this first for any LogKV work.**

### Shared
- `dataset.py` - Data pipeline with memmap caching. Tokenizes HF datasets, packs short documents into context-length sequences.
- `predict.py` / `predict_stream.py` - Generation / interactive streaming REPL. `_load_model` picks the architecture from config.json's `model_type` ("logkv" → LogKVLM); also detects legacy pipeline checkpoints (`full_model.pt`).
- `chat_server.py` - Chat web UI (legacy-model era; decaying repetition penalty, reset/interrupt).

### Legacy (RecursiveCompressor)
- `recursive_compressor.py`, `recursive_compressor_lm.py`, `recursive_compressor_lm_pipeline.py`, `configuration_recursive_compressor.py`, `train_pipeline.py` (6-GPU pipeline parallel, Schedule1F1B). History: `doc/copying-memory-branch-changes.md`; full experiment logs under `doc/instruction-for-claude/`.

## Commands
```bash
uv sync                                                # Install dependencies
uv run pytest test_logkv.py test_logkv_lm.py -v        # LogKV tests
uv run pytest test_lm.py -v                            # Legacy tests

# LogKV standard-config training (DDP, 6 GPUs)
uv run torchrun --nproc_per_node=6 train_logkv.py --run-name <name> \
    --phase-emb --phase-levels 2 --gated-attention --self-slot

uv run python predict_logkv.py --model-dir $DATA_DIR/checkpoints_logkv/<name>/checkpoint-<step>/model \
    --max-new-tokens 1024 --temperature 0.7 --top-p 0.9
uv run python predict_stream.py --model-dir /path/to/checkpoint --temperature 0.7 --top-p 0.9

uv run torchrun --nproc_per_node=6 train_pipeline.py   # legacy pipeline-parallel training
```

## Training Control
```bash
just pause / just resume / just save-and-exit   # writes control.cmd (pause keeps GPUs allocated but idle)
```
Resume flags must match the run's original flags (model structure comes from the checkpoint's config.json).

## TensorBoard
`train/loss`, `train/grad_norm`, `train/lr` per step: LogKV runs under `$DATA_DIR/tensorboard/logkv-{dataset_type}/{run}/`, legacy under `$DATA_DIR/tensorboard/{dataset_type}/`.
```bash
uv run tensorboard --logdir $DATA_DIR/tensorboard/
```

## Environment
- `.env` file sets `DATA_DIR` (datasets, checkpoints, memmap caches)
- Default: `DATA_DIR=./data`; Production: `DATA_DIR=/mnt/raid0/RecursiveCompressor`
- Hardware: 6x RTX 3090 (24GB VRAM each), 256GB RAM

## Key Design Decisions — LogKV
Details and evidence live in `doc/logkv.md`; summary:
- **Refined layout (2026-09-06)** removes overlapping previous-block slots. Matched 50k-step experiments (one seed, phase2 + gated + self slot) preserve perfect Copying through T=131072 (41 horizons, n=256, best/final), with an additional 8/8 at T=16777216, but reduce Selective Copying accuracy (T=64 best string: 30.5% → 17.6%). See `doc/logkv-refine-experiments.md`. LM quality and the LM throughput measurements below still belong to the overlapping layout. Weight shapes/config flags are unchanged, so old checkpoints load with new attention semantics. Restart generation with hidden=None; old runtime hidden states are incompatible.
- **Standard config**: fixed level decay (−i·log C) + phase embedding (levels=2, period 16) + multi-head + gated attention + self slot (the query's own k/v as one extra slot = standard causal-mask semantics; loss-neutral, gives the softmax an "attend to nothing" option, stabilizes grad_norm ~1.14→0.74). `kv_norm`/`learnable_decay`/`level_amplify`/`v_norm_only` exist as options but are NOT standard (each was tested and rejected for the LM: kv_norm caps the key-norm retrieval margin, learnable decay and amplification worsen temp-0.7 repetition, v_norm alone helps little).
- **Level decay** originally corrected cross-level multiplicity and improved topic fixation. It is retained as a coarse-level penalty in the refined layout, where slots no longer overlap; its former multiplicity rationale no longer applies. The original 16.7M-token Copying result is in §6.13; a separate refined-layout probe is recorded in `doc/logkv-refine-experiments.md`. The variable-memory study in `doc/logkv-position-study.md` ablates decay only with the new combined positional scheme. Decay ablations for standard phase2 and LM quality remain untested.
- **Relative position bias**: optional `--relative-position-bias` adds `-(j-c-1)*log(C)/(C-1)` to every level's valid slot logits, on top of the level penalty. Nearest completed sub-unit has distance 0. Self slot stays at bias 0; compression pooling is unchanged. No new parameters/state. To replace phase embeddings, omit `--phase-emb`. Config default is False for old checkpoints; see `doc/logkv.md` §2.4.
- **Relative-bias experiment (2026-09-07)**: all 6 matched 50k-step runs are fully evaluated. Retrieval-only relative bias without phase embeddings loses perfect Copying (T=131072 best/final string 0%) and trails phase2 on Selective Copying. Compression-side position bias was not part of this experiment. See `doc/logkv-relative-position-experiments.md`.
- **Compressor position decay**: `--compressor-decay α` subtracts `α*(C-1-c)` from pooling logits; default 0 preserves old behavior. `--learnable-compressor-decay` uses positive per-layer/head softplus slopes shared across hierarchy levels, initialized geometrically from α to α/4 (positive α required). Flattened rows have order (batch, head, chunk), so `num_chunks` must be passed to Compressor. K/V share the biased weights; q_out stays chunk-last. No hidden-format change; see `doc/logkv.md` §2.5. The independent test oracle includes pooling as well as retrieval.
- **Compressor experiment (2026-09-07)**: all 6 runs (fixed half/base and learned × both tasks, phase off/RPE on) completed 50k steps and best/final evaluation. Learned Copying improves T=64 string to 41.02% / 13.28% from retrieval-only 3.52% / 0.78%, but T=131072 remains 0%; results vary by horizon/checkpoint. Selective T=64 is 0% / 0.39% for all three modes, below phase2. Keep standard phase2 and compressor decay default 0. One seed; learned initialization also differs across heads. 221 tests passed. See `doc/logkv-compressor-experiments.md` and §6.20.
- **Position study**: experimental `--compressor-position-transform` binds child position into K/V using fixed permutations plus two learned Householder reflections (shared across hierarchy levels); `--relative-position-kv` adds local relative K/V vectors including self distance 0; `--level-decay-scale 0` ablates fixed level decay. Defaults preserve old behavior. The 6-mode × 2-task variable-M/prefix study completed 50k steps per run and 5,280 evaluation cells (one seed). Exact Copying remains unsolved; Selective improves in some short-horizon cases. See `doc/logkv-position-study.md` for all best/final results and limitations; code is in `exp/position_study/`. 265 tests passed.
- **Phase embedding** (base-C digits of the absolute position, small period) breaks the positional degeneracy inside runs of identical tokens (multi-scale windows coincide there, making "how many so far" uncountable). Longer periods aliased beyond the training range and hurt extrapolation in earlier configurations; period 16 was the robust choice there. The variable-memory study documents its limits for longer payloads and does not establish arbitrary-length exact copying.
- **step()/forward()/predict() equivalence** is the core invariant, tested at fp64 <1e-12 against an independent `reference_forward` in `test_logkv.py` (forward delegates to step, so the test oracle must stay independent). Any semantic change (biases, norms, gates) must be mirrored in the reference.
- **Hidden format**: `(levels, offset)`; `levels[i] = [cur_q, cur_k, cur_v]` holds only the unfinished chunk (<C entries), with heads folded into the batch dim. Offset reconstructs absolute positions (phase, block bases). Retained slices are cloned to avoid pinning full-segment storage during inference. O(C·log L·d) memory.
- **Online softmax + activation checkpointing**: the whole attention pass is one non-reentrant checkpoint region; autograd keeps only per-level contexts (~1.33·L·d) instead of (L, C·levels, d) slot gathers. bf16-weight inference (no autocast) needs the softmax weights cast to the value dtype (fixed; see test).
- VRAM/throughput at d1024/8H/16L/ctx2048 (~310M params): batch 4/GPU ≈ 16 GiB, ~16-17k tok/s total on 6 GPUs (~4h per 5000 steps).

## Key Design Decisions — data pipeline & training (shared)
- **Tokenizer**: `elyza/ELYZA-japanese-Llama-2-7b-fast`. `[INST]`, `[/INST]` are Llama-2-style plain text markers (not special tokens).
- **Data format** (Llama 2 style): Documents: `<s>text</s>`. Conversations: `<s>[INST]q1[/INST]a1</s><s>[INST]q2[/INST]a2</s>...` (each turn BOS+EOS-wrapped).
- **Pretrain chunking + packing** (`_build_memmap_packed`): `[BOS] + tokens + [EOS]` split into context_length chunks (first chunk has `<s>`, last has `</s>`, continuations unmarked); chunks packed to fill samples, PAD-filled; loss on all non-PAD positions.
- **Instruct conversations** (`_build_memmap_conversations`): prompt `<s>[INST]q[/INST]` and answer `a</s>` tokenized separately for an exact loss-mask boundary; answer-only loss mask in a parallel `.mask` memmap; no cross-conversation packing.
- **Memmap caching**: uint16 memmaps under `$DATA_DIR/hf_cache/mmap/ctx{context_length}/`; per-source version suffix (pretrain `_v5`, instruct `_v6`) — bump a suffix to force rebuild. `prefault=True` warms the OS page cache on rank 0 (shared across ranks).
- **All-PAD-label NaN guard**: `_pack_chunks` enforces `MIN_CONTENT=2`; loss functions also return `logits.sum() * 0.0` for all-PAD (micro)batches (0/0 CE = NaN otherwise).
- **Mixed precision**: fp32 master weights/optimizer state, bf16 autocast forward/backward. RMSNorm computes in fp32 but outputs bf16. CE loss gets `logits.float()`.
- **Optimizers**: Muon for 2D hidden Linear weights (`adjust_lr_fn="match_rms_adamw"`), AdamW for embedding/head/biases/norms and non-2D params (`_ADAMW_ONLY_KEYWORDS` includes `phase_emb`; Muon rejects non-2D tensors).
- **Sampler shuffle**: `DistributedSampler` seed=0 + `set_epoch` gives a reproducible order; train_logkv resume skips consumed samples via a SkipSampler.
- **Legacy pipeline notes** (train_pipeline.py): Schedule1F1B loss collection via `losses=[]`, per-stage checkpoints + reconstructed `full_model.pt`, `STAGE_LAYER_SPLIT` for VRAM balance, per-step ReduceLROnPlateau on EMA loss. Cache building happens before `init_process_group` (sentinel file); all ranks barrier after checkpoint saves.

## Debugging Guidelines
- After modifying LogKV, run `uv run pytest test_logkv.py test_logkv_lm.py -v` before committing; the fp64 reference/step/predict equivalences must stay <1e-12.
- When modifying the data pipeline (packing, collation), add shape/length assertions; all packed sequences must be exactly context_length (`(seq + [PAD] * context_length)[:context_length]`).
- **NaN debugging**: training is deterministic with `seed=0` + `set_epoch(epoch)`, so resuming reproduces the same NaN at the same step. Loss=NaN with finite GradNorm typically means all-PAD labels (0/0 CE), not bad logits.
- Generation-quality checks use the repetition metric suite (temp 0.7/1.0 × 3 seeds × 1024 tokens: EOS rate, Q4 bigram distinct ratio, heavy-repetition count, longest same-char run) — see doc/logkv.md §6.6 for baselines.
- Legacy tests `test_step_split_consistency`/`test_predict_matches_forward` use loose tolerances (`atol=5e-3`) due to data-dependent xs propagation.

## Current Model Parameters (LogKV standard)
- d_model=1024, num_heads=8, d_ff=3072, chunk_size=4, num_layers=16 (~310M params)
- phase_emb=True (phase_levels=2), gated_attention=True, self_slot=True; decay fixed at log C
- context_length=2048, lr=2e-4 (linear warmup 1000), DDP batch 4/GPU × 6 GPUs
- mixed precision: fp32 master / bfloat16 autocast
- (Legacy pipeline model: d_model=2048, num_heads=16, d_ff=6144, num_layers=16, lr=5e-5)

## Datasets
Selected by `--dataset-type` in `train_logkv.py` / `train_pipeline.py`:
- `pretrain` (documents only):
  - `wikimedia/wikipedia` (20231101.ja, 20231101.en)
  - `hotchpotch/cc100-ja-documents`
  - `JeanKaddour/minipile`
- `instruct` (conversations only):
  - `shi3z/ja_conv_wikipedia_llama2pro8b_30k`
  - `shi3z/ja_conv_wikipedia_orion14B_100K`
  - `HuggingFaceH4/ultrachat_200k`
